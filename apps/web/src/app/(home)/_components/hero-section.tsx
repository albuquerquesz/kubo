"use client";

import { CTASection } from "@/components/ui/hero-dithering-card";

/** Home hero — dithering CTA card is the default composition. */
export default function HeroSection() {
  return <CTASection />;
}
