{
  "mcpServers": {
    "kubojs": {
      "command": "npx",
      "args": ["-y", "kubojs@latest", "mcp"],
      "note": "Official kubojs MCP server (stdio). Exposes bts_get_stack_guidance, bts_get_schema, bts_plan_project, bts_create_project, bts_plan_addons, and bts_add_addons. Runs via npx; no API key or auth required."
    }
  }
}
